# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (Pixel ID / webhook URL are not secret, but keeping
# the habit of never committing env files avoids leaking real credentials
# if a CAPI access token ever ends up here by mistake)
.env
.env.*
!.env.example

# Reference material we were given, not something we built
*.pdf

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
